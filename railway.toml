[build]
builder = "DOCKERFILE"
dockerfilePath = "./Dockerfile"
dockerBuildArgs = ["VITE_GOOGLE_CLIENT_ID"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
